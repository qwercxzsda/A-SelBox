name: A-SelBox
channels:
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
      - python-amazon-sp-api
      - python-dotenv
      - pandas
      - "psycopg[binary]"

      # Formatting tools
      - sqlfluff
      - mdformat
      - mdformat-gfm
      - pymarkdownlint
      - ruff
