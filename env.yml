name: A-SelBox
channels:
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
      - amzn-sp-api
      - python-amazon-sp-api
      - python-dotenv
      - pandas
      - "psycopg[binary, pool]"

      # Formatting tools
      - sqlfluff
      - mdformat
      - mdformat-gfm
      - pymarkdownlnt
      - ruff
      - pyright[nodejs]
