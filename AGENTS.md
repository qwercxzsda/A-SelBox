# AGENTS.md

- Do not read or modify the `.env` file. The file contains sensitive credentials.
- Use the conda environment `A-SelBox` to run the python scripts.
- In case of the need for additional packages, install them in the `A-SelBox` environment and update the `env.yml` file.
- Type annotate if possible. Format files after modification.
- Read the README.md files for additional information.
